# assets/models

glTF (`.glb`/`.gltf`) part library for the 3D builder and simulator. Loaded via
drei's `useGLTF`.

Planned V1 parts:

| Part | File | Notes |
|------|------|-------|
| 4WD chassis | `chassis_4wd.glb` | parametric mount points |
| Wheel | `wheel.glb` | instanced ×4 |
| ESP32 DevKit | `esp32.glb` | |
| L298N driver | `l298n.glb` | |
| HC-SR04 ultrasonic | `hcsr04.glb` | front mount |
| Servo | `servo.glb` | |
| Battery pack | `battery.glb` | |

Keep models low-poly and in **metres** (Three.js / Rapier world units). License
each asset CC0 / CC-BY where possible and record provenance here.
